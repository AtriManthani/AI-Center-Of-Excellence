# Structured Record Templates

These JSON templates show the fields required for new use-case, license, and issue records. Replace every angle-bracket placeholder and validate the completed record before moving it into `inventory/data/`.

Templates are not inventory records and are intentionally excluded from dashboard calculations.
