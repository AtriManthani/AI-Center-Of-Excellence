# Controlled Vocabularies

This folder contains machine-readable values used by schemas, validation, dashboards, and GitHub automation. Change a value only through a controlled review because existing records and reports may depend on it.

`vocabularies.json` is the canonical list. Display labels may be added without replacing stable stored values.
