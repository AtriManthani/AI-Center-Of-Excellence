# Lifecycle Mappings

This folder connects the canonical governance lifecycle to inventory reporting buckets and substages. The mapping allows governance and executive reporting to use different levels of detail without changing the underlying decision state.

`lifecycle-mapping.json` is used by validation and dashboard generation. Changes require governance and inventory review.
