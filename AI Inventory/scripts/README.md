# Inventory scripts

`export_inventory.py` validates every `status.json` record, calculates health, and builds an allowlisted leadership dataset for automated checks.

The live dashboard discovers approved use-case records directly from this repository, so filename or folder changes do not require a dashboard redesign.
