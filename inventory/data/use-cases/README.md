# Use-Case Records

Add one validated JSON file per use case, named with its permanent ID and a short slug, for example `AI-0042-permit-assistant.json`.

Records must conform to `shared/schemas/use-case.schema.json`, use approved vocabulary values, and follow `shared/mappings/lifecycle-mapping.json`. A production record must include monitoring ownership.
