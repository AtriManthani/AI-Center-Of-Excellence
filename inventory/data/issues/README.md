# Operational Issue Records

Add one validated JSON file per operational issue, named with its issue ID and a short slug, for example `ISS-0012-output-quality.json`.

Every issue must reference an existing live use-case record. Sensitive incident evidence must remain in its approved incident-management system.
