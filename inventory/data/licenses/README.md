# License Records

Add one validated JSON file per approved license pool, named with its permanent license ID and a short slug, for example `LIC-0003-approved-tool.json`.

For every record, `purchased` must equal `assigned + available`. Use the approved product name and do not store individual assignee names unless specifically authorized.
