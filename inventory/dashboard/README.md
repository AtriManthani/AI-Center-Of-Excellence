# Inventory Dashboard

`AI-INVENTORY.md` is generated from approved JSON records in `inventory/data/`. Do not edit calculated sections by hand.

Run:

```text
python tools/generate_dashboard.py
```

The validation workflow checks that the committed dashboard matches the current records. The dashboard is intentionally Markdown-first so it remains reviewable, portable, and accessible before a dedicated application or business-intelligence integration is selected.
