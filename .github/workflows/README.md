# GitHub Actions Workflows

`validate.yml` runs dependency-free repository validation, automated tests, and the dashboard freshness check on pushes and pull requests.

The workflow has read-only repository permissions and does not modify records or make governance decisions.
